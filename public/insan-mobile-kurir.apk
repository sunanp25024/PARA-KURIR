
PK

This is a placeholder APK file for INSAN MOBILE Kurir application.

IMPORTANT: This is NOT a real installable APK file!

To generate the actual APK file that can be installed on Android devices:

1. Export project to Github via "Export to Github" button
2. Clone the project to your local machine
3. Run: npm install
4. Run: npx cap add android
5. Run: npm run build
6. Run: npx cap sync
7. Run: npx cap open android
8. In Android Studio: Build > Generate Signed Bundle/APK
9. Replace this placeholder with the generated APK file

For testing purposes, you can also build an unsigned APK:
- In Android Studio: Build > Build Bundle(s)/APK(s) > Build APK(s)

The real APK will be generated in: android/app/build/outputs/apk/

